name: my_function
description: An app using Dart with Cloud Functions for Firebase
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  firebase_admin_sdk: ^0.5.3
  firebase_functions: ^0.6.0

dependency_overrides:
  firebase_admin_sdk:
    git:
      url: https://github.com/firebase/firebase-admin-dart.git
      path: packages/firebase_admin_sdk
      ref: 0eb09ac86e30f5fb0e2a91814984045d0b13801d

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^6.0.0
